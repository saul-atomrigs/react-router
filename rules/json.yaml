id: json
language: "json"
rule:
  kind: string_content
  pattern: $PKG
  regex: "@remix-run\\/"
  not:
    regex: "@remix-run\\/router"
transform:
  PKG:
    replace:
      by: "@react-router/"
      replace: "@remix-run\\/"
      source: $PKG
fix: $PKG
